# Expected verdict — fragment 10 (Gaming Dispersion)

**Expected: not yet established under the promoted D prompt.**

This fragment's only recorded Phase 4 runs are the step-5 A/B/C calls (2026-06-08), all of which returned STRESS_TEST under the baseline-A attractor (D4.6) — those outputs are not valid expectations for the post-D baseline. An expected verdict is an operator ruling against the promoted prompt's verdict definitions, not a transcription of model output; establishing one here requires that ruling (optionally corroborated by a run under the live prompt), which has not been made. Until then this fragment is a fixture without an assertion.

Note: this fragment is also the thin-input drift case behind item 18 (route-level parse-guards) — its Phase 3 output drifted to YAML and its Phase 4 B output drifted to prose. It earns its place in the suite as the format-drift fixture independent of any verdict assertion.

Sidecar — not a harness input (the `phase-*` allow-list in `scripts/prompt-harness/run.ts` excludes this file by name).
