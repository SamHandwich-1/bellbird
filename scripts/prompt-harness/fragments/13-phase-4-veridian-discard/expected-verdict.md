# Expected verdict — fragment 13 (Veridian Therapeutics)

**Expected: DISCARD**

This is the true-DISCARD boundary case for the Phase 4 adjudication prompt under the ruled DISCARD reading:

> Broken premise with live adjacent expression → STRESS_TEST.
> DISCARD only for dead insight / nothing recoverable.

Both DISCARD legs are exercised by this fragment:

1. **Broken structural premise, no recoverable expression of the bet.** The single-asset binary has resolved against the thesis — VRD-101 missed its Phase 3 primary endpoint, and the mechanistic class is invalidated by the contemporaneous peer readout. The thesis as constructed has no live alternative expression: no second asset, no peer survivor, no scarcity or second-order angle.

2. **Dead insight with no expression that recovers edge.** Residual cash-shell value sits below the thesis entry price; the company faces dilution or wind-down. The "edge" the thesis was pricing is no longer present in any tradeable form.

This sits structurally distinct from fragment 12 (mall-REITs), where the hedge note itself names a live adjacent expression — the A-mall scarcity angle — which routes to STRESS_TEST under the ruling. Veridian carries no such carve-out.

If D returns anything other than DISCARD on this fragment, the prompt has not encoded the ruled reading.

Sidecar — not a harness input (the `phase-*` allow-list in `scripts/prompt-harness/run.ts` excludes this file by name).
