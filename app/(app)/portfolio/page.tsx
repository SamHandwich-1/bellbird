import { StubScreen } from '@/components/shared/StubScreen';

export default function PortfolioPage() {
  return (
    <StubScreen
      mode="Portfolio"
      turn="B"
      note="Target allocation by thesis with correlation clusters — rebuilding in Turn B against the v2 mockup."
    />
  );
}
