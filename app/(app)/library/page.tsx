import { StubScreen } from '@/components/shared/StubScreen';

export default function LibraryPage() {
  return (
    <StubScreen
      mode="Library"
      turn="B"
      note="Thesis library, multi-select, print/share — rebuilding in Turn B against the v2 mockup."
    />
  );
}
