import { StubScreen } from '@/components/shared/StubScreen';

export default function ThesisDetailPage() {
  return (
    <StubScreen
      mode="Thesis"
      turn="B"
      note="Thesis detail view, suggested-positions table with expandable notes, transcript viewer — rebuilding in Turn B."
    />
  );
}
