import { StubScreen } from '@/components/shared/StubScreen';

export default function DevelopPage() {
  return (
    <StubScreen
      mode="Develop"
      turn="B"
      note="Four-phase pipeline with voice, attachments, and prompt prefills — rebuilding in Turn B against the v2 mockup."
    />
  );
}
