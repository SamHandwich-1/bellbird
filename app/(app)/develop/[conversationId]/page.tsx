import { StubScreen } from '@/components/shared/StubScreen';

export default function ConversationPage() {
  return (
    <StubScreen
      mode="Conversation"
      turn="B"
      note="Develop conversation with split chat + context pane, voice input, attachments — rebuilding in Turn B."
    />
  );
}
